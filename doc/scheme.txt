TuxKatana
 ├── MainWindow
 │    ├── Settings            # TabPage ╕
 │    │     ├── Presets             # Presets Tab  
 │    │     ├── Amplifier(Effect)    
 │    │     ├── Booster(Effect)
 │    │     ├── Mod(Effect)
 │    │     ├── Fx(Effect)          # Mod Copy
 │    │     ├── Delay(Effect)
 │    │     ├── Reverb(Effect)
 │    │     ├── DelayReverb         # Delay Copy
 │    │     └── Chains              # TODO
 │    └── Switcher
 │          ├── Effects       # BOOSTER/MOD/FX/DELAY/REVERB
 │          └── Channels      # CH_1 => CH_8
 └── Controller
      ├── Port                      # Midi Comm
      ├── SysEx(MIDIBytes)          # SysEx Message
      ├── Memory                    # Amp Mry Dump
      └── Presets                   

####################################################
  ┌────────┐
  │ Katana │
  └┬───────┘
   │       ▲
   │ sysex │
   ▼       │
  recv    send
  ┌─────────┴──┐
  │ Controller │
  └┬───────────┘
   │       ▲
   │       │
   ▼       │
  set     get
  ┌────────┴─┐
  │  Memory  │
  ├──────────┤
  │  address │
  └────┬─────┘
   bind value
  ┌────┴─────┐
  │ property │ <- params/<effect>.yaml
  ├──────────┤
  │  Effect  │ <- inherited from Amplifier, Booster, Mod, Fx, Delay, Reverb
  └──────────┘
